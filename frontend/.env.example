NEXT_PUBLIC_API_URL=http://localhost:3001

# Stripe test-mode publishable key (pk_test_...). Leave blank to use the mock
# payment flow. Never put the SECRET key here — it belongs only in backend/.env.
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
